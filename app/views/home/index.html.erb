<div class='container-fluid bg-light home_container'> 
  <div class='intro-text'>Expense Tracker App <br/><span>Having Control on your expenditure</span></div>
  <div class='action'>
  <%= link_to "Sign up now!", new_user_registration_path, class: 'btn btn-secondary p-3  button'%>
  <%= link_to "Log In", new_user_session_path, class: 'btn btn-primary p-3 ms-5 button'%>
  <div>
  <% if user_signed_in? %>
    <%= button_to 'logout', destroy_user_session_path, method: :delete, class: 'btn btn-danger' %>
  <% end %>
    </div>

